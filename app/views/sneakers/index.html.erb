<div class="container index">
  <div class="description-example d-flex justify-content-between">
    <p>This is the world rack, where you can see all users sneakers and if you like one of this sneaker but you missed a drop just check if is avaible in our shop 👉 👉 👉
    </p>
    <%= link_to "Shop", root_path, class: "btn btn-shop-index" %>
  </div>
  <div class="button-add text-center">
    <% if policy(Sneaker).create? %>
      <%= link_to new_sneaker_path do %>
        <i class="fas fa-plus-circle add"></i>
      <% end %>
    <% end %>
  </div>
  <div class="row">
    <div class="col">
          <div class="cards">
            <% @sneakers.each do |sneaker| %>
              <%= link_to sneaker_path(sneaker) do %>
                <div class="img-cards" id="image-cards">
                  <%= cl_image_tag sneaker.photo, class: "img-show-sneaker" %>
                </div>
              <% end %>
            <% end %>
          </div>
    </div>
  </div>
</div>


<!-- <script type="text/javascript">
  (function() {

  var img = document.getElementById('image-cards').firstChild;
  img.onload = function() {
      if(img.height > img.width) {
          img.height = '500px';
          img.width = 'auto';
      }
  };
}());
</script> -->
